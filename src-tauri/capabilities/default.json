{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "enables the default permissions",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "dialog:allow-confirm",
    "vault",
    "hash-vault-password",
    "hash-vault-password-legacy",
    "verify-vault-password",
    "verify-vault-password-legacy"
  ]
}
